# CPU Core Allocation Guide for BenderServer HFT Infrastructure

> **Документ**: Руководство по распределению CPU ядер  
> **Версия**: 2.0  
> **Дата обновления**: Декабрь 2024

---

## 📋 Содержание

1. [Обзор](#обзор)
2. [CPU Mapper Tool](#cpu-mapper-tool)
3. [Правила распределения ядер](#правила-распределения-ядер)
   - [OS / Housekeeping](#1-os--housekeeping)
   - [Network Stack / IRQ](#2-network-stack--irq)
   - [Gateways](#3-gateways)
   - [AR + RF + Formula](#4-ar--rf--formula)
   - [Trash + ClickHouse](#5-trash--clickhouse)
   - [UDP Handler](#6-udp-handler)
   - [Robots](#7-robots)
4. [Стратегия L3 Cache Partitioning](#стратегия-l3-cache-partitioning)
5. [Multi-Socket Topology](#multi-socket-topology)
6. [Диагностические команды](#диагностические-команды)
7. [Ресурсы](#ресурсы)

---

## Обзор

Правильное распределение CPU ядер критически важно для минимизации latency в HFT системах. Ключевые принципы:

| Принцип | Описание |
|---------|----------|
| **NUMA Locality** | Размещение компонентов на NUMA-ноде сетевой карты |
| **L3 Isolation** | Разделение "горячих" и "холодных" процессов для минимизации cache eviction |
| **Cross-NUMA Avoidance** | Избегание межнодового трафика где возможно |
| **Jitter Reduction** | Изоляция критических ядер от ОС и IRQ |

---

## CPU Mapper Tool

### 🔗 Ссылка на инструмент

**[HFT CPU Mapper](https://asciimrk-commits.github.io/goffy_cpu/)**

### Возможности

| Функция | Описание |
|---------|----------|
| **Topology Visualization** | Интерактивная карта сокетов, NUMA нод, L3 кэшей и ядер |
| **Role Painting** | Назначение ролей на ядра кликом мыши |
| **Config Export** | Генерация YAML конфигурации для Ansible (`bs_instances`) |
| **Compare View** | Сравнение конфигураций двух серверов |
| **Auto-Optimize** | Автоматическая генерация оптимальной раскладки по правилам |

### Быстрый старт

1. Выполните `cpu-map.sh <hostname>` на сервере
2. Вставьте вывод в CPU Mapper
3. Нажмите **Build Map**
4. Используйте **Auto-Optimize** или распределите ядра вручную
5. Скопируйте конфигурацию

---

## Правила распределения ядер

### 1. OS / Housekeeping

> **Назначение**: Системные процессы, kernel, housekeeping

| Параметр | Значение |
|----------|----------|
| **Расположение** | Не-изолированные ядра (обычно 0-N) |
| **Количество** | 2-8 ядер в зависимости от load average |
| **Целевая нагрузка** | ~20% (проверять через `mpstat`, `htop`) |

**Правила:**

- Использовать "парные" ядра (0+его hyperthread)
- На 2+ NUMA системах: брать ядра с обоих socket для балансировки
- Серверы 100+ ядер: 4-5 ядер
- Серверы ~12 ядер: 1 ядро

---

### 2. Network Stack / IRQ

> **Назначение**: Обработка сетевых прерываний

| Параметр | Значение |
|----------|----------|
| **Расположение** | Строго на NUMA-ноде сетевой карты |
| **Количество** | 1-2 ядра |
| **Требования** | Изолированные ядра |

**Правила:**

- Привязать очереди (Queues) сетевой карты к этим ядрам
- Настроить RX и TX affinity (включая XPS)
- **Никогда** не размещать на одном ядре с Gateways/Robots

---

### 3. Gateways

> **Назначение**: Критический путь торговых сообщений

| Параметр | Значение |
|----------|----------|
| **Расположение** | NUMA-нода сетевой карты |
| **Режим** | Dedicated Core (выделенное ядро) |
| **Целевая нагрузка** | ~20% AVG |
| **Tier** | SSS+ (чистый L3 кэш) |

**Правила:**

- Максимальный приоритет по L3 locality
- Не делить L3 с Trash/ClickHouse
- При нехватке ядер: использовать Tier 2 (соседний L3)

---

### 4. AR + RF + Formula

> **Назначение**: AllRobots Thread, RemoteFormula, Formula

| Компонент | Правила |
|-----------|---------|
| **AR (AllRobots)** | Отдельное ядро, **НИКОГДА** не с Trash |
| **RF (RemoteFormula)** | Может быть с AR или с Trash |
| **Formula** | Обычно на том же ядре что и AR |

**Критическое правило:**

```
⚠️ AR + Formula НИКОГДА не размещать на одном ядре с Trash
```

---

### 5. Trash + ClickHouse

> **Назначение**: Сбор мусорных данных, аналитика

| Параметр | Значение |
|----------|----------|
| **Расположение** | Сервисный L3 пул (отдельный от Gateways) |
| **Совмещение** | Trash + RF + ClickHouse могут быть вместе |

**Правила:**

- Выделить "сервисный" L3 кэш для этих задач
- Не смешивать с trading-critical компонентами
- Может делить ядро с UDP (если UDP не активен)

---

### 6. UDP Handler

> **Назначение**: Обработка UDP трафика

| Параметр | Значение |
|----------|----------|
| **Триггер** | Активировать при 10+ тыс. pps |
| **Проверка** | `sar -n UDP 1` |
| **Количество** | Максимум 1 ядро |

**Правила:**

- Размещать на NUMA-ноде сетевой карты
- При Remote MDR — обязательно выделять
- Может делить ядро с Trash (если UDP неактивен)

---

### 7. Robots

> **Назначение**: Торговые роботы

| Tier | Описание | Приоритет |
|------|----------|-----------|
| **Diamond** | Чистый L3, та же нода что и Gateways | 🥇 Высший |
| **Gold/Silver** | Соседняя NUMA на том же сокете | 🥈 Средний |
| **Bronze** | Cross-socket (через UPI/QPI) | 🥉 Низкий |

**Правила:**

- Использовать Robot Pools для группировки
- Dedicated Core режим
- Целевая нагрузка: ~20% AVG

---

## Стратегия L3 Cache Partitioning

### Принцип

- **"Горячие" данные**: Gateways, Robots → Чистый L3
- **"Холодные" данные**: Trash, ClickHouse, RF → Отдельный L3

### Пример для сервера 16 ядер (2 L3 кэша по 8 ядер)

| L3 Pool | Ядра | Назначение |
|---------|------|------------|
| **Сервисный** | 1-8 | Trash, UDP, IRQ, AR+RF, Gateways Tier A |
| **Рабочий** | 9-16 | Gateways SSS+, Robots |

### Пример распределения сервисного L3

| Ядро | Роль | Примечание |
|------|------|------------|
| 1 | Trash + RF + ClickHouse | Сервисный пул |
| 2 | UDP | Если активен |
| 3-4 | IRQ | Сетевые прерывания |
| 5 | AR + Formula | Не с Trash! |
| 6-8 | Gateways Tier A | Если не хватает чистых L3 |

---

## Multi-Socket Topology

### Иерархия NUMA-нод (Tier List)

Пример: Network на Node 2

| Tier | NUMA Node | Latency | Использование |
|------|-----------|---------|---------------|
| **Tier 1** | Node 2 (Network) | Минимальный | Gateways, IRQ, критические Robots |
| **Tier 2** | Node 1 (Same Socket) | Низкий | Robots Pool 1, дополнительные Gateways |
| **Tier 3** | Node 3, 4 (Cross-Socket) | Высокий | Robots Pool 2, низкоприоритетные задачи |

### Важно для Tier 3

- Использовать только если не хватает ядер на локальном сокете
- Cross-socket трафик через UPI/QPI добавляет latency
- Подходит для Robots Pool 3, Pool 4

---

## Диагностические команды

```bash
# CPU topology и текущие привязки
sudo bender-cpuinfo
bender-cpuinfo -o net   # Сетевые ядра

# NUMA topology
numactl -H

# Полная CPU topology
lscpu -e

# Мониторинг нагрузки по ядрам (softirq, sys, usr)
mpstat -A 15 1

# UDP статистика (проверка необходимости UDP ядра)
sar -n UDP 15 1
```

---

## Ресурсы

| Ресурс | Ссылка |
|--------|--------|
| **CPU Mapper Tool** | [https://asciimrk-commits.github.io/goffy_cpu/](https://asciimrk-commits.github.io/goffy_cpu/) |
| **Performance Guide** | [https://github.com/alexkachanov/performance](https://github.com/alexkachanov/performance) |
| **HFT Analytics Dashboard** | [Grafana Dashboard](https://metrics.qb.loc/d/bender_hft_analytics_v2/bender-hft-analytics-detailed-v2) |

---

## Changelog

| Версия | Дата | Изменения |
|--------|------|-----------|
| 2.0 | Декабрь 2024 | Полная реструктуризация, добавлен CPU Mapper, таблицы правил |
| 1.0 | — | Исходная версия |
